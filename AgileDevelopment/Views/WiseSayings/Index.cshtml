@model IEnumerable<AgileDevelopment.Models.WiseSaying>

@{
	ViewData["Title"] = "Wise Sayings";
}

<h1>@ViewBag.Title</h1>

<p>
	<a asp-action="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Title)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Methodology)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Title)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Methodology.Title)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.WiseSayingID">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.WiseSayingID">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.WiseSayingID">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
<div>
	<a asp-controller="Methodologies" asp-action="Index">Back_to_Home</a>
</div>
